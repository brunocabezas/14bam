@import "./styles/constants";

.app
  padding 0
  min-height 100%

  .appContainer
    min-height 100%
    display flex
    flex-direction column

    .appContent
      flex 10
      height "calc(100% - %s)" % ($header_height)
