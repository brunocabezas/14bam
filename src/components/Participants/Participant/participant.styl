@import '../../../styles/colors'

.participant
  .pageTitle
    text-decoration underline

  .pageLeft
    .participanGallery
      height 250px
      background-size cover
      background-position center center
      display flex
      align-items center
